
// В доме N этажей, М подъездов, на каждом этаже К(=1) квартир
//По номеру квартиры определить:
// m - номер подъезда
// n - номер этажа
// k - порядковый номер квартиры на этаже считается слева направо
//где расположена искомая квартира
#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

int main()
{
    int N = 5;//этажей
    int M = 3;//подъездов
    int K = 1;//количество квартир на этаже
    int m;    //номер подъезда
    int n;    //номер этажа
    int k;    // порядковый номер квартиры
    //int dom[N][M];
    setlocale(LC_ALL, "Russian");
//----------------------------------------------------------------
    for(int i = 1; i <= N; i++)
    {
        for(int j = 1; j <= M; j++)
        {
            dom[i][j] = i * j);
            printf("%2d ",dom[i][j]);
        }

        printf("\n");

    }
    
    printf("\n");
    
    for(int j = 1; i <= M; i++)
    {
        for(int i = 1; i <= N; ++)
        {
            dom[i][j] = i * j);
            printf("%2d ",dom[i][j]);
        }

        printf("\n");

    }
//----------------------------------------------------------------
/*printf("В доме этажей %d, подъездов %d,"
       "на каждом этаже квартир %d\n", N,M,K);
printf("Введите номер интересующей вас квартиры: ");
scanf("%d", &k);


printf("Квартира номер %d находиться в подъезде № %d"
       " на этаже № %d\n", k, n,m);*/

    return 0;
}
